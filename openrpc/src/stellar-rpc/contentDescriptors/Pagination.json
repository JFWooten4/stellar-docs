{
    "PaginationEvents": {
        "name": "pagination",
        "summary": "pagination options",
        "description": "Pagination in stellar-rpc is similar to pagination in Horizon. See the Pagination docs.",
        "required": false,
        "schema": {
            "$ref": "#/components/schemas/PaginationEvents"
        }
    },
    "PaginationLedgers": {
        "name": "pagination",
        "summary": "pagination options",
        "description": "Pagination in stellar-rpc is similar to pagination in Horizon. See the Pagination docs.",
        "required": false,
        "schema": {
            "$ref": "#/components/schemas/PaginationLedgers"
        }
    },
    "PaginationTransactions": {
        "name": "pagination",
        "summary": "pagination options",
        "description": "Pagination in stellar-rpc is similar to pagination in Horizon. See the Pagination docs.",
        "required": false,
        "schema": {
            "$ref": "#/components/schemas/PaginationTransactions"
        }
    }
}
